[project]
name = "digital-me"
version = "0.1.0"
description = "A simple digital twin chatbot using Gradio and OpenAI"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "gradio==5.34.2",
    "huggingface-hub[cli]>=1.2.4",
    "openai",
    "python-dotenv",
    "requests>=2.32.5",
]

[tool.uv]
managed = true
